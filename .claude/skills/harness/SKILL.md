---
name: harness
description: 이 프로젝트의 하네스(`.claude/**`의 스킬·에이전트·훅·rules·settings)를 추가·제거·수정·검토·관리하는 모든 작업에 사용한다. 실제로 편집하지 않는 검토·논의도 포함한다. 하네스를 사용해 수행하는 일반 개발 작업은 대상이 아니다.
---

# harness

목적: 하네스 구성요소가 아래 원칙과 배치 기준을 따르도록 추가·제거·수정·검토를 수행한다.

## 원칙

### 제1원칙 — 목적이 있어야 한다

- 구성요소의 본문 또는 스크립트 최상단에 목적을 주석(또는 첫 문장)으로 서술한다.
- 목적은 모호하지 않고 간결하게 완결돼야 하며, 하나의 구성요소는 복수의 목적을 가지지 않는다.
- 목적을 서술할 수 없으면 구성요소로 추가하는 것을 재고한다. 목적이 다른 구성요소와 겹치면 병합 또는 확장을 고려한다.

### 제2원칙 — 필요한 문장만 쓴다

- 문장마다 판정한다: **이 문장을 지우면 목적 달성이 실패하는가.** 실패하지 않으면 지운다. "있으면 좋다"는 탈락이다.
- 쓰지 않는 것: 배경 설명, 규칙의 정당화, 일반론적 모범 사례, 이미 다른 곳(코드·문서·다른 구성요소)에 있는 내용 — 마지막은 포인터로 대체한다.
- 판단이 갈리는 지점에만 판정 기준을 한 줄 붙인다.

### 제3원칙 — 경계를 명시한다

목적이 미치는 범위와 미치지 않는 범위를 함께 서술한다.

### 제4원칙 — 검증 가능해야 한다

- 구성요소가 목적을 실제로 달성하는지, 그 목적을 위한 요청에 적절히 호출되는지 검증할 방법을 서술한다.
- 구성요소를 추가·수정한 시점에 그 검증을 실제로 수행한다.

### 제5원칙 — 호출 조건은 자신이 정의한다 (description)

- 구성요소는 자신이 언제 호출될지를 description에 서술한다.
- 다른 구성요소의 호출 조건은 알 필요가 없다. 명시적으로 특정 구성요소를 호출하는 것은 가능하나, 그 경우에도 상대의 호출 조건은 알 필요가 없다.
- 호출 시점·조건이 적절한지는 구성 시점에 판단한다. 실행 시점에 자체적으로 판단하지 않는다.

## 배치 기준

| 조건 | 배치처 |
|---|---|
| 모든 세션에서 항상 필요한, 변경 빈도 낮은 지침·사실 | CLAUDE.md (200줄 이하 유지, 멀티스텝 절차는 두지 않음) |
| 적용 조건이 작업 종류가 아니라 파일 경로·패턴 | `.claude/rules/` (frontmatter `paths:`로 해당 파일을 읽을 때만 로드) |
| 특정 종류의 작업에서 반복되는 멀티스텝 절차 | 스킬 (본문은 호출 시에만 로드) |
| 독립 컨텍스트가 필요한 작업 — 중간 출력이 커서 격리하거나, 병렬로 돌리는 조사·검토 | 에이전트 |
| 모델 판단에 의존하면 안 되는 결정적 강제 — 포맷팅, 검증, 차단 | 훅 (settings.json) |
| 사용자가 명시 호출할 때만 실행할 절차 | 스킬 + `disable-model-invocation: true` |

CLAUDE.md에서 절차로 자라난 내용은 스킬로 옮긴다.

## 피드백 반영

- 하네스 검토·수정 시 `.claude/FEEDBACK.md`의 미해결 기록을 먼저 확인한다.
- 사용자가 요청하거나 같은 구성요소에 기록이 3건 이상 쌓이면 개선을 제안한다.
- 개선에 반영한 기록은 FEEDBACK.md에서 삭제한다.

## 모델 교체 재검토

`.claude/model-review.txt`에 `pending`인 모델이 있으면 수행한다. 대상은 모델에 종속된 항목뿐이며, 하네스 전체를 다시 읽지 않는다.

1. `.claude/agents/*.md`의 `model:` 별칭 — 별칭이 가리키는 세대가 바뀌었는지 확인하고 작업 난이도에 맞는지 다시 판정한다.
2. `references/agents.md`의 모델 ID 목록 — 실제 사용 가능한 ID와 대조한다.
3. `.claude/FEEDBACK.md`의 미해결 기록 — 새 모델에서도 재현되는지 확인해 보고한다.

완료하면 해당 줄을 `reviewed`로 바꾼다.

## 경계

- 대상: `.claude/**`와 CLAUDE.md에 대한 추가·제거·수정·검토·관리.
- 비대상: 하네스를 사용해 수행하는 일반 개발 작업, 하네스 밖의 코드·문서.

## 검증

- 목적 달성: 변경한 구성요소가 위 5원칙과 배치 기준을 각각 만족하는지 항목별로 대조한다. 본문에 명령·스크립트를 적었으면 실제로 실행해 본다.
- 호출 적절성: 이 스킬은 하네스 관련 요청(추가·수정·검토 등)에서 로드되고, 일반 개발 요청에서는 로드되지 않아야 한다.

## references

구성요소 종류별 작성법. 각 reference는 **템플릿을 반드시 포함한다.**

- 에이전트 작성: [references/agents.md](references/agents.md)
- 스킬 작성: [references/skills.md](references/skills.md)
- 훅 작성: [references/hooks.md](references/hooks.md)
- rules 작성: [references/rules.md](references/rules.md)
- CLAUDE.md 작성: [references/claude-md.md](references/claude-md.md)
- settings 작성: [references/settings.md](references/settings.md)
